# Vendored browser libraries for Demo SDK scenes

- `gsap.min.js` — GSAP 3.12.7 core
- `ScrollTrigger.min.js` — GSAP ScrollTrigger plugin

Text splitting for verse reveals is implemented in `playground/static/sdk/demo.js` (no SplitText plugin required).

Downloaded from jsDelivr for offline use on the workstation.
